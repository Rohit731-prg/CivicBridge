import { Text } from "react-native"
import { ScrollView } from "react-native"

function signup() {
  return (
    <ScrollView>
        <Text>Signup</Text>
    </ScrollView>
  )
}

export default signup